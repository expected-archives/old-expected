syntax = "proto3";

package protocol;

message CreateContainerRequest {
    string id = 1;
}

message ChangeContainerStateRequest {
    string id = 1;

    enum RequestedState {
        START = 0;
        STOP = 1;
    }

    RequestedState requestedState = 2;
}
